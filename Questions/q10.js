// For a given array of numbers, print the squares of the numbers using  forEach loop.
const numbers = [1, 2, 3, 4, 5];
function square(value){
    console.log(value*value);
}
numbers.forEach(square);
